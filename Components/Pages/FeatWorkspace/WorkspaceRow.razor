<div class="flex items-center between space-x-4 p-[12px] rounded-[12px] hover:bg-sBackground transition-all ease-in-out cursor-pointer">
    <div class="h-[48px] w-[48px] rounded-[4px] bg-tBackground"></div>

    <div class="flex flex-col">
        <span class="font-semibold text-[18px]">
            @Workspace.Name
        </span>

        <span class="text-sText">
            @Workspace.Description
        </span>
    </div>
</div>

@code {
    [Parameter]
    public Workspace Workspace { get; set; } = default!;
}